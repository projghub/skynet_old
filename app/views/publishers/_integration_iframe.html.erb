<iframe src="<%= url_for :controller => "serve", :action => "index", :only_path => false, :pub => publisher.access_hash, :tid => publisher_integration.tid, :a => attribute_value_ids_to_hash(publisher_integration.attribute_value_ids) %>" style="border:0px;margin:0px;padding:0px;width:<%= template.width %>px;height:<%= template.height %>px;overflow:hidden;"></iframe>
