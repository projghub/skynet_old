<%= form_for(@ad_group) do |f| %>
  <% if @ad_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_group.errors.count, "error") %> prohibited this ad_group from being saved:</h2>

      <ul>
      <% @ad_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :campaign_id %><br />
	 <%= f.select :campaign_id, Campaign.all.collect{ |c| [c.name, c.id] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
